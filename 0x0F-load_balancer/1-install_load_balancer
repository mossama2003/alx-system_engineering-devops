#!/usr/bin/env bash
<<<<<<< HEAD
# install haproxy
sudo apt-get update
sudo apt-get install -y haproxy
echo "frontend web-front
    bind *:80
    default_backend web-back

backend web-back
    balance roundrobin
    server web1 34.229.137.179:80 check
    server web2 54.160.84.19:80 check
" | sudo tee -a /etc/haproxy/haproxy.cfg
sudo service haproxy restart
=======
# install load balancer


echo -e "Updating and doing some minor checks...\n"

function install() {
	command -v "$1" &> /dev/null

	#shellcheck disable=SC2181
	if [ $? -ne 0 ]; then
		echo -e "	Installing: $1\n"
		sudo apt-get update -y -qq && \
			sudo apt-get install -y "$1" -qq
		echo -e "\n"
	else
		echo -e "	${1} is already installed.\n"
	fi
}

install haproxy #install haproxy

echo -e "\nSetting up some minor stuff.\n"

# backup default server config file
sudo cp /etc/haproxy/haproxy.cfg haproxy_default.backup

server_config=\
"
defaults
  mode http
  timeout client 15s
  timeout connect 10s
  timeout server 15s
  timeout http-request 10s

frontend ma2-tech-frontend
    bind *:80
    default_backend ma2-tech-backend

backend ma2-tech-backend
    balance roundrobin
    server 417671-web-01 54.152.138.146:80 check
    server 417671-web-02 554.236.44.97:80 check
"

# shellcheck disable=SC2154
echo "$server_config" | sudo dd status=none of=/etc/haproxy/haproxy.cfg

# enable haproxy to be started by init script
echo "ENABLED=1" | sudo dd status=none of=/etc/default/haproxy

echo "configured - Roundrobin On web-01 & web-02"

if [ "$(pgrep -c haproxy)" -le 0 ]; then
	sudo service haproxy start
else
	sudo service haproxy restart
fi
>>>>>>> 48536dad2a29c6f8d91330affa50cd9d5cf87e3e
